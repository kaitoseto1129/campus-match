-- Supabase Dashboard > SQL Editor で一度だけ実行してください(このリポジトリでは実際には MCP 経由で個別に実行済み)。
-- トーク画面の動作確認用に、Kaitoアカウントへダミーからのマッチを3件作成し、
-- 既存のダミーマッチ(まえまえざわ側)と合わせて計11件の挨拶メッセージ(ダミー側からの送信)を投入。
-- 実際に投入した具体的なmatch_id/sender_id/本文はこのリポジトリでは個別実行のため割愛。
-- 再現する場合は、対象アカウントのmatchesを確認し、各match_idに対して
-- ダミー側user_idをsender_idとしたmessagesレコードをinsertすること。
